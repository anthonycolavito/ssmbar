% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/reform_functions.R
\name{pia_reform}
\alias{pia_reform}
\title{PIA Calculation (Reform-Capable)}
\usage{
pia_reform(worker, assumptions, debugg = FALSE)
}
\arguments{
\item{worker}{Dataframe with a worker's earnings and AIME by year and age}

\item{assumptions}{Dataframe with the Social Security Trustees historical and projected economic variables and program parameters}

\item{debugg}{Boolean value that directs function to output additional variables if set to true}
}
\value{
worker dataframe with a worker's retired worker PIA by age
}
\description{
Function that computes a worker's Primary Insurance Amount by age with
support for reform parameters including PIA multipliers (Reform #1),
flat benefits (Reform #2), 4th bracket (Reform #3, #12-14), and
mini-PIA blending (Reform #22).
}
\details{
Compares the regular PIA (bend point formula) with the special minimum PIA
per 42 USC 415(a)(1)(C) and returns the higher of the two.

This is the reform-capable version. For baseline (current law) PIA calculation,
see \code{\link{pia}}.
}
\seealso{
\code{\link{pia}} for the baseline version
}
